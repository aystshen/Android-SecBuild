# NAME		Relative path
#
#HWDEF		HWDEF
package-file	package-file
bootloader	../outputs/MiniLoaderAll.bin
parameter	../outputs/parameter.txt
trust       ../outputs/trust.img
uboot       ../outputs/uboot.img
misc		../outputs/pcba_small_misc.img
resource	../outputs/resource.img
#kernel		../outputs/kernel.img
#boot       ../outputs/boot.img
recovery	../outputs/recovery.img
#system		../outputs/system.img
#vendor		../outputs/vendor.img
#oem		../outputs/oem.img
#lcdparam	../outputs/lcdparam.img
# 要写入backup分区的文件就是自身（update.img）
# SELF 是关键字，表示升级文件（update.img）自身
# 在生成升级文件时，不加入SELF文件的内容，但在头部信息中有记录
# 在解包升级文件时，不解包SELF文件的内容。
backup		RESERVED
#update-script	update-script
#recover-script	recover-script
